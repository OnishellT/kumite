# Copilot Review Instructions

Review Kumite changes as a Go CLI and installer project.

Focus on:
- correctness of generated project files and install behavior;
- branch/release safety;
- scoped edits with no generated cache or build artifacts committed;
- tests and static-analysis coverage for changed behavior;
- clear installation and workflow documentation.

Do not request broad rewrites when a narrow fix is enough.
